{
	"name": "tweeter-server",
	"version": "1.0.0",
	"description": "",
	"license": "ISC",
	"author": "",
	"type": "commonjs",
	"main": "index.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"clean": "powershell -Command \"if (Test-Path dist) { Remove-Item -Recurse -Force dist }\"",
		"compile": "npm run clean && tsc",
		"seed": "npm run compile && node dist/scripts/SeedData.js",
		"seed:large-followers": "npm run compile && node dist/scripts/SeedLargeFollowers.js"
	},
	"dependencies": {
		"@aws-sdk/client-sqs": "^3.1021.0",
		"@aws-sdk/client-dynamodb": "^3.1021.0",
		"@aws-sdk/client-s3": "^3.1021.0",
		"@aws-sdk/lib-dynamodb": "^3.1021.0",
		"bcryptjs": "^3.0.3",
		"date-fns": "^3.3.1",
		"tweeter-shared": "file:../tweeter-shared",
		"uuid": "^9.0.1"
	},
	"devDependencies": {
		"@types/bcryptjs": "^2.4.6",
		"@types/node": "^25.5.0"
	}
}
